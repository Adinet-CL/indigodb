{
  "name": "@adinet/indigodb-cli",
  "version": "2.0.0",
  "private": true,
  "bin": {
    "indigodb": "dist/index.js"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --out-dir dist",
    "pretest": "pnpm run build",
    "test": "vitest run --config=vitest.config.ts",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "inquirer": "^12.6.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.3.3",
    "vitest": "^3.1.2"
  }
}